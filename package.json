{
  "name": "weather-widget",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Widget thời tiết dùng Open-Meteo, kèm API sinh SVG để nhúng vào README",
  "license": "MIT",
  "engines": {
    "node": "22.x"
  },
  "scripts": {
    "dev": "node dev-server.mjs",
    "typecheck": "tsc --noEmit && node scripts/check-config-sync.mjs"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "@vercel/node": "^5.0.0",
    "typescript": "^5.7.0"
  }
}
